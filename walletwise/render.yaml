services:
  - type: web
    name: walletwise
    env: java
    buildCommand: mvn clean package -DskipTests
    startCommand: java -jar target/pfm-1.0.0.jar
    envVars:
      - key: JAVA_VERSION
        value: 17
